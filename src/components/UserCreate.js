import React from 'react';
import Field from './Field';

import Button from './Button';

// Creación del formulario para el nombre de registro.

const UserCreate = () => {
  return (
    <div className="ui form">
      <Field />
      <Button />
    </div>
  );
};

export default UserCreate;
